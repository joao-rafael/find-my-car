export enum SnackbarMessages {
    success = 'Data loaded successfully!',
    fail = 'Network/API error. Check the console.',
    clear = 'Filter cleared.',
    empty = 'No available data for the selected filters.'
}